# AI4RadX NMOpti Extension

핵의학 최적화(NM Optimization)를 위한 3D Slicer 기반 확장 모듈입니다.

## Structure
- `config/`: Extension specific configurations
- `src/`: Python source code for Slicer modules
- `resources/`: UI and other assets

## Development
This extension is mounted to `/app` inside the container.
